<div class="flex-wrapper">
    <div class="link-back">
        {{{ BackLink }}}
    </div>
    <form class="profile-data">
        {{{ AvatarComponent }}}

        <div>
            {{{ EmailItem }}}
            {{{ LoginItem }}}
            {{{ NameItem }}}
            {{{ SurnameItem }}}
            {{{ DisplayNameItem }}}
            {{{ PhoneItem }}}
        </div>

        {{{ SaveButton }}}
    </form>
</div>

