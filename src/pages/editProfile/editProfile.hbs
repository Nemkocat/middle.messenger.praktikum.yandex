<div class="flex-wrapper">
    <div class="link-back">
        {{{ BackLink }}}
    </div>
    <form class="profile-data">
        {{{ AvatarComponent }}}

        <div>
            {{{ EmailItem }}}
            {{{ LoginItem }}}
            {{{ NameItem }}}
            {{{ SurnameItem }}}
            {{{ NicknameItem }}}
            {{{ PhoneItem }}}
        </div>

        {{{ SaveButton }}}
    </form>
</div>

