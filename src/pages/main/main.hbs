<main class="flex-wrapper">
    <div class="chats-menu">
        <nav class="chats-menu-nav">
            {{{ ProfileLink }}}
            
            <form class="chats-menu-nav__form">
                {{{ SearchInput }}}
            </form>
        </nav>
            {{{ ChatList chats=chats }}}
    </div>

    <div class="picked-chat">
        {{{ ChatArea chat=mockChats}}}
    </div>
</main>

